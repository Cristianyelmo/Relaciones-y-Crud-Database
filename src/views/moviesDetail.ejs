<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= movie.title %> </h1>    
    <br>
    <p>RATING: <%= movie.rating %> </p>
    <p>AWARDS: <%= movie.awards %> </p>
    <p>LENGTH: <%= movie.length + ' min'%> </p>
    <p>RELEASE DATE: <%= movie.release_date %> </p>
    <p>RELEASE DATE: <%= movie.release_date %> </p>
     
<% if (movie.generos) { %>
    <a href="/genres/detail/<%= movie.generos.id %>"><%= movie.generos.name %></a>
 
<% }else {%>
<p>Sin especificcar</p>

    <% }%></p>


    <p>ACTORES:</p>


<% movie.actores.forEach(movie => { %>
<li><a href="/movies/actors/<%= movie.id %>"><%= movie.first_name %></a></li>

<% }) %>












    
    <br>
    <section>
     <a href="/movies/edit/<%= movie.id %>">Editar</a>
     <a href="/movies/delete/<%= movie.id %>">Eliminar</a>      
    </section>

</body>
</html>